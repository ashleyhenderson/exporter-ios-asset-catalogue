{* Generate path from segments. Path remains the same *}
{[ let path = (asset.group.path.append(asset.group.name).append((asset.originalName + ".colorset")).join("/")) /]}

{* Generate asset path *}
Assets.xcassets/{{ path + "/" }}